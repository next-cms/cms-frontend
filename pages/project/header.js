/* eslint-disable react/prop-types */
import React from "react";

const HeaderSetting = ({ brand, navbar }) => {
    return (
        <div>
            header
        </div>
    );
};
export default HeaderSetting;
